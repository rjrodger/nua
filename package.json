{
  "name": "nua",
  "version": "2.1.2",
  "description": "Reference-preserving merge",
  "main": "dist/nua.js",
  "types": "dist/nua.d.ts",
  "scripts": {
    "test": "node --enable-source-maps --test \"dist-test/**/*.test.js\"",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test \"dist-test/**/*.test.js\"",
    "watch": "tsc --build src test -w",
    "build": "tsc --build src test",
    "test-web": "npm run build && browserify -o test-web/test-web.js -e dist-test/nua.test.js -s Nua -im -i assert && open test-web/index.html",
    "coveralls": "lab -s -P test -r lcov | coveralls",
    "prettier": "prettier --write --no-semi --single-quote src/*.ts test/*.ts",
    "clean": "rm -rf dist dist-test node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": " npm run build && npm run test && npm run repo-tag && npm publish --registry http://registry.npmjs.org --access public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rjrodger/nua.git"
  },
  "author": "Richard Rodger (richardrodger.com)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rjrodger/nua/issues"
  },
  "homepage": "https://github.com/rjrodger/nua#readme",
  "devDependencies": {
    "@hapi/code": "^9.0.3",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  },
  "files": [
    "src",
    "dist",
    "LICENSE"
  ]
}
